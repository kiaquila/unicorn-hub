# Minimal Target Repository

This synthetic example exists only for bootstrap smoke tests and documentation. It intentionally contains no real product details, credentials, domains, or infrastructure identifiers.
