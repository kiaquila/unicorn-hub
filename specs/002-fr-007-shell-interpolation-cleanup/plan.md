# Plan: FR-007 Shell Interpolation Cleanup

## Summary

Move the remaining `${{ github.event_name }}` interpolation in `ai-review.yml` and the five `${{ ... }}` interpolations in `pr-guard.yml`'s `Resolve diff refs` step into `env:` blocks, then re-sync root workflows from templates.

## Technical Context

- Source of truth: `templates/.github/workflows/`.
- Root mirror: regenerated by `scripts/sync-workflows.mjs`.
- Affected steps: `Resolve selected review policy` (ai-review), `Resolve diff refs` (pr-guard).

## Constitution Check

- Spec-first: this folder records the cleanup before the workflow edits land.
- Testable boundaries: covered by `pnpm run preflight` (which calls `sync-workflows.mjs --check` and the existing parity test) and by visual inspection that no `${{ }}` remains inside `run:` blocks in the touched files.
- PR-only: changes ship on `fix/fr-007-env-mapping-consistency`.
- Simplicity: pure mechanical refactor — no new scripts, no new dependencies.
- Deployability: no behavioural change at runtime; values are identical, just routed through environment variables.

## Complexity Tracking

None. The change is a one-to-one substitution that brings the previously missed shell blocks under the same hardening pattern that `Resolve selected review policy` already uses for `vars.AI_REVIEW_AGENT` and `inputs.trigger_mode`.

## Verification

- `pnpm run preflight`
- `node scripts/sync-workflows.mjs --check`
- Manual grep: `${{ }}` must not appear inside any `run:` block in `templates/.github/workflows/{ai-review,pr-guard}.yml`.

## Risks

- None functional. Only risk is a typo in the env variable name (caught by preflight syntax check and review).
